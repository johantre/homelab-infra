[Unit]
Description=Watch NetAlertX config for changes
After=docker.service

[Path]
PathChanged={{ netalertx_config_dir }}/app.conf
Unit=pihole-backup-flag.service

[Install]
WantedBy=multi-user.target
